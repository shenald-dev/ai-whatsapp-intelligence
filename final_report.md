Updated backend/app/api/endpoints.py to use func.count() instead of func.count(models.Message.id) for improved database query performance on dashboard group stats endpoint. Bumped version to 1.0.29 across components and updated lock files.
